#include<iostream>
using namespace std;
int main(){
    char ch;
    cin>>ch;
    int x=(int)ch; // Typecasting the char ch to integer x
    cout<<x;
}
// This program takes a character input from the user, converts it to its corresponding ASCII integer value using typecasting, and then outputs that integer value.